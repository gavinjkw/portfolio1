import { IGroupByRecord } from './groupby-record.interface';
import { ISortingExpression } from './sorting-expression.interface';
import { IgxSorting } from './sorting-strategy';
export interface IGroupByResult {
    data: any[];
    metadata: IGroupByRecord[];
}
export declare class IgxGrouping extends IgxSorting {
    groupBy(data: any[], expressions: ISortingExpression[], grid?: any, groupsRecords?: any[]): IGroupByResult;
}
